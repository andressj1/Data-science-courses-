A. Clic Izq en Tabla empleados
B. Seleccionar "Nueva Medida"

1. Total De Alguna Categoría Sin Repetidos
Total Empleados = COUNTROWS('Tabla_Empleados')

2. Promedio 
Sueldo Prom = AVERAGE('Tabla_Empleados'[Sueldo])

3. Suma
Total Finanzas = SUM('Finanzas'[Cantidad])

4. Calculate
Ingresos = CALCULATE(SUM('Finanzas'[Cantidad]), Finanzas[Tipo] = "Ingresos")
	# Le dice a DAX: “Calcula [Cantidad] pero bajo ciertas condiciones que te voy a dar”.

5. Resta
Utilidad = [Ingresos]-[Gastos]

6. Division 
Cuota= [Gastos] / [Presupuesto]

7. Saldo (Compleja)
Saldo = CALCULATE( [Utilidad], FILTER( ALL(Finanzas) , Finanzas[Fecha] <= MAX(Finanzas[Fecha]))) 

	# ALL(Finanzas): Quita todos los filtros que existan sobre la tabla Finanzas. Es decir, ve toda la tabla sin ningún filtro activo.
	# FILTER(... , Finanzas[Fecha] <= MAX(Finanzas[Fecha])): Luego reaplica un filtro personalizado: incluye solo las filas donde la fecha sea menor o
	         igual a la fecha máxima del contexto actual.

--------------------------------------------------------
A. Crear una nueva columna;  

3. Calcular Edad	
Edad = INT(YEARFRAC('Tabla_Empleados'[Nacimiento], TODAY()))

-INT convierte en entero 

4. Columna Condicional
Grupo_Edad =
SWITCH(
	TRUE(),
	'Tabla_Empleados'[Edad] <=30, "20-30",
	'Tabla_Empleados'[Edad] <=40, "30-40",
	"Mas 40"
)


--------------------------------------------------------
Ultima actualización
1. Obtener datos 
2. Consulta en blanco
3. Escribir; 
= #table(type table[Ultima Actualización=datetime], {{DateTime.LocalNow()}})